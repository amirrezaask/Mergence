import { ToolSessionApp } from "./tools/ToolSessionApp.js"

/** Compatibility entry kept for imports from older integrations. */
export default function LegacyAppRoot() {
  return <ToolSessionApp />
}
