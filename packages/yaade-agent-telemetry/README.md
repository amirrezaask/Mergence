# `@yaade/agent-telemetry`

This package is YAADE's **CLI agent telemetry** subsystem. It observes
hooks/plugins from terminal-launched agents and reduces them into activity,
unread, attention, and notification projections.

The legacy `@yaade/agent-telemetry` package re-exports this package for compatibility.
